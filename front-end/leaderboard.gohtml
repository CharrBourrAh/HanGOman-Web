<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Leaderboard</title>
    <link rel="stylesheet" href="/front-end/leaderboard.css">
<body>
<div class="leaderboard">
    <h1>🏆 Classement</h1>
    <ul class="leaderboard-list">
        {{range $index, $player := .}}
            {{ if gt $player.Attempts 0 }}
            <li class="leaderboard-item">
                <span class="rank">{{add $index 1}}</span>
                <span class="Nickname">{{.Nickname}}</span>
                <span class="Remain Attempts">{{.Attempts}} pts</span>
            </li>
        {{end}}
    </ul>
</div>
</body>
</html>